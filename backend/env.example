PORT=3000

DB_HOST=db
DB_USER=postgres
DB_PASSWORD=
DB_NAME=periferia
DB_PORT=5432

JWT_SECRET=**********
ADMINER_PORT=8081

DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}